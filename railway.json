{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "rm -f public/hot && npm run build"
    },
    "deploy": {
        "preDeployCommand": "rm -f public/hot && php artisan migrate --force"
    }
}
